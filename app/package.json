{
  "name": "job-finder-app",
  "private": true,
  "version": "1.0.0",
  "description": "Job Finder dashboard — Express API + React frontend for tracking a remote IT-support job search.",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend start",
    "gcal-auth": "npm --prefix backend run gcal-auth"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
